import 'package:flutter/material.dart';

class StepWatch extends StatelessWidget {
  const StepWatch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Watch-Modus kommt in einer spaeteren Phase.');
  }
}
